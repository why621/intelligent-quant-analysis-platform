# 开发规格与变更记录

更新日期：2026-09-09。首次补建代码基线：`codex/regression-deployment` / `d9f0798`。

## CR-016 截图复核与新会话续作交接（2026-09-09）

- 当次范围：对照用户四张预览截图、现有代码和开发记录，核实MVP缺口，更新原交接文档及SDD执行顺序。仅文档变更；不实现业务、不访问云端、不推送或部署。
- 核查基线：本地`codex/regression-deployment`，HEAD `3d8bc3f`，开始时工作树干净。历史云观测及旧测试不算本轮重新验证。
- 关联REQ-02/07/08/09/10：登记T-022跨日发布一致性问题；继续既有T-016f、T-017至020，不重做已通过的M0、不重新询问已确认范围。
- 验收结果：原交接文档已增加唯一当前入口、证据/阶段门槛，并替换旧启动语、标记历史记录；PowerShell检查六份文档138个本地链接均有效、UTF-8无替换字符、代码块成对，WSL `git diff --check`通过。`git diff --stat`及`git status --short`确认仅六份Markdown修改；未提交/推送，未执行本轮业务测试、联网验证、云检查或部署。T-022及完整MVP各待办没有标为完成。

本文是项目SDD（规格驱动开发）的入口与变更台账。基线来自当前代码、仓库文档、用户提供的申请书/需求文档和本次讨论，不代表所有目标已实现。

## 文档分工

| 文档 | 职责 | 更新时机 |
| --- | --- | --- |
| [PRD.md](PRD.md) | 范围、需求编号、验收标准 | 需求确认或范围变化 |
| [design.md](design.md) | 当前架构、目标设计、技术决策、迁移风险 | 实现前及设计变化 |
| [task.md](task.md) | 任务、依赖、状态和验证证据 | 开始工作和交付时 |
| 本文 | 变更动机、批准边界、影响、验证与回滚 | 每个有意义的变更批次 |

接口字段、路径和枚举仍以 [OpenAPI](packages/contracts/openapi.yaml) 及共享Schema为机器可读依据。旧 [架构说明](docs/architecture.md) 与 [部署故障记录](deploy/regression/data-resilience.md) 保留细节和历史背景；带日期的旧状态不覆盖本基线。代码、契约和需求冲突时先登记差异，不只修改文字宣称完成。

## 工作流程

2026-09-08用户明确确认：此后整个项目严格按SDD流程开发。该要求同时写入根目录AGENTS.md；先规格、后实现、经验证后回写是每轮开发门槛，不是可选建议。

1. 登记`CR-编号`，说明动机、批准范围和非目标。
2. 在PRD确定受影响的`REQ-编号`和验收标准，区分用户确认与提案。
3. 在design记录方案、契约/存储变化、测试及回滚方式。
4. 在task拆分`T-编号`、依赖和完成条件；待决事项仅阻塞相关实施。
5. 按小补丁逐个审阅实施；接口变化同步契约、调用方和测试。
6. 记录实际运行命令、环境、结果与限制，再更新状态。离线测试、源可达、容器健康、完整业务验收分别记录。
7. 提交、推送、部署及付费服务遵守当次授权；文档提案不自动授权外部操作。

状态统一为：待处理、进行中、待确认、受阻、完成。完成必须有证据，部分成功不能标记整体完成；后续变更追加记录，不改写历史失败。

## 当前基线

| 项目 | 已知状态 | 证据及限制 |
| --- | --- | --- |
| 业务 | Vue/Flask、缓存、相关性、两种传统策略、异步回测、排行、模拟配置已有代码 | 不等于真实数据和公网五模块验收通过 |
| 资产范围 | 50只股票/ETF静态混合池 | `akshare_provider.py`的`_DEFAULT_UNIVERSE`；不是沪深300成分库 |
| 数据链路 | 历史主路径为AkShare/Tencent，概览核心为东方财富全市场列表 | 沪深300新方案尚未实施 |
| 回归部署 | CR-015云前后端af02f826预览；本地后续文档HEAD为3d8bc3f | 最后核实2026-09-08；保留50只旧混合池，概览仍503。2026-09-09仅核查截图/代码，未重新检查云端，非完整MVP |
| 上游故障 | 新IP下东方财富TCP 3/3成功、行情0/6成功；新浪总数/两条行情6/6成功 | 2026-09-07只读观测，不能证明全量/长期稳定或确定故障责任方 |
| 发布 | 最终方向为Pages前端+云后端 | 云前端仅临时回归入口；域名/HTTPS/Pages完整联调尚未验收 |
| AI/海外/实盘 | 后续或范围外 | 不宣称已实现AI、海外股票或真实下单 |

CR-016最新缺口与执行顺序见 [新会话交接](docs/session-handoff-2026-09-08.md) 顶部：T-022跨日发布一致性 → T-016f正常无交易分类 → 有界扩采/独立指数/统一发布 → T-017概览 → T-018五模块 → T-019运维 → T-020/T-009最终验收。现有50池四类研究结果不等于完整300只MVP；历史批次状态不覆盖该入口。

## 变更台账

### CR-015 可运行功能云端预览部署（2026-09-08，本批完成）

- 用户当次授权：先将能运行的部分部署现有云服务器并提供网址。本批发布已验证的M0页面、模拟配置边界和API0.4.0目录修复；保留默认50只混合池、旧ETF和现有缓存。
- 不启用未完成的300只候选行情/新概览，不推送GitHub或发布Pages，不购买服务；云HTTP前端仍为临时内部演示入口，不代表HTTPS或完整MVP验收。
- 先复跑离线/构建，检查云端干净版本及数据/镜像备份；发布对应前后端代码，保留回滚镜像，不重复执行旧000成交量迁移、不删除卷。云回归允许少量模拟回测任务，不产生真实交易。
- 验收分开记录公网页面、真实HTTP/浏览器默认操作、任务恢复及概览已知503；部署失败恢复原镜像，数据不作无条件整库覆盖。结果回写本台账和独立部署报告。
- 发布审阅补记：旧首页介绍把AI/概览描述为已覆盖，本批改成实际可体验功能并增加阶段预览边界，不以功能占位冒充完整MVP。首次备份遇Windows文本stdin转CRLF，旧服务未替换；修复为UTF-8字节后新r2目录两库integrity_check均ok。
- 实际af02f826前后端部署，公网浏览器13场景及股票HTTP回归通过，Python320+部署工具6、前端52和构建通过。旧50池13275条行情及元数据不变，新增4个研究测试任务；概览503/截止9月7日如实保留。见 [部署交付](docs/cr015-cloud-preview-2026-09-08.md)，非完整MVP。

### CR-014 云服务器腾讯历史只读复测（2026-09-08，本批完成）

- 用户当次明确要求先测试云服务器；暂停T-016f实现，本轮只验证43.161.223.91实际后端容器的腾讯历史链路，不部署/推送/安装依赖/更新生产缓存。
- SSH使用同一实例旧IP43.161.219.65已保存公钥进行严格校验，实际新IP登录成功；不关闭主机校验、不展示私钥。
- 复用CR-013六只名单样本与2025-09-07至2026-09-07区间：每只最多5次实际HTTPS请求、40秒硬截止、串行间隔1秒，整轮最多30请求；无重试、不重新下载名单、不抓全市场。
- 通过stdin在现有容器内存执行已测试的请求限制与覆盖评估，使用已部署provider的日线适配函数而不构造存储；禁写Python字节缓存，输出回传本机artifacts。额外只读核对版本/镜像及health/status/overview，不以已有缓存冒充刚采集成功。
- 结果分别记录云端返回、覆盖和与本地证据的差异；东方财富恢复、全300、跨日稳定性、云端MVP均不由本次腾讯样本推定。
- 实际19:18在云后端容器完成：6只真实返回、18次HTTP全部200，五只242日/688981为241日，与本地1451条逐条一致。线上health200，旧50池status仍stale、概览503；未触发东方财富采集。32项诊断离线/静态检查通过，无部署/缓存写入，见 [云复测报告](docs/cr014-cloud-history-2026-09-08.md)。

### CR-013 逐资产覆盖与隔离采集（2026-09-08，本批完成；M2进行中）

- 用户批准继续M2；关联REQ-02/09、T-016d/e。先实现只用于隔离验证的覆盖manifest，不切默认50池、不发布HTTP覆盖接口、不推送/部署或写线上数据。
- 复用CR-012已校验名单，不重复下载。颗粒度为名单版本+股票身份+前复权+请求区间；记录每只实际/预期交易日、缺口、非交易日、重复/无效数值、金额缺失和来源证据。缺口原因未知保留unknown，不猜停牌/上市、不补价。
- 外部验证先最多6只、每只最多5个实际HTTP请求（包含SDK隐藏请求），合计最多30；串行间隔1秒、单只40秒、批次300秒。只允许已核实腾讯HTTPS主机，禁止环境代理/重定向/重试；失败不自动换源。先离线验证再执行。
- 结果与原始SDK规范化观察值保存在新artifacts目录，全部300只均在manifest中，未采集明确not_attempted；完整样本仅进入隔离SQLite，部分/无效样本不当可用历史。金额可空，不据价格覆盖称概览成交额完整。
- 验收：离线缺失/重复/非法OHLCV/边界/预算/输出范围检查，限量真实源验证，离线重放及伴随质量笔记本；完整300/停牌证据/独立指数/生产发布保持未完成。
- 实际：Python317/前端52、静态检查与质量笔记本通过。腾讯18次HTTP、6只均返回，5只242日完整、688981为241日；300分母未缩减，294未采集。已从官方复牌公告核实缺2025-09-08为停牌，但自动规则未消费该证据，仍gaps、不补价、不入可用库。零网络重放一致；详见 [CR-013报告](docs/cr013-history-validation-2026-09-08.md)。下一批先T-016f停牌/上市/代码变更规则，再扩采；无推送/部署。

### CR-012 可追溯沪深300名单快照（2026-09-08，本批完成）

- 用户要求说明MVP剩余项并继续；本批推进REQ-01/02/09、T-004/005/015/016，不推送、不部署、不改线上。
- 先实现官方名单快照的严格解析、唯一身份/日期/300条校验、原始文件哈希与可重复读取；提供显式隔离资产池接入，不自动替换现有默认50只或删除ETF/历史。
- 外部验证预算：最多2次官方名单下载，每次连接5秒、读取10秒、总进程45秒，禁止重定向和无限重试；原文件/快照仅保存artifacts新目录，不入库，不抓300只行情。
- 验收分开记录：离线畸形/重复/日期/交易所/篡改检查，真实名单质量检查，可追溯快照→完整目录API本地验证。没有逐资产一年行情、独立指数和概览时不宣称MVP完成。
- 使用可复现质量检查及伴随笔记本保存证据；许可、云端源可用性、全量行情、HTTPS和跨日运维仍是独立门槛。
- 测试依赖补记：完整JSON Schema校验依赖jsonschema，原虚拟环境未安装；保留完整检查并加入开发依赖，不将运行时策略参数子集校验当作替代。
- 首次名单HTTP已返回，解析因实际列名不匹配拒绝；第二次（预算内最后一次下载）先留存原文件/时间/哈希，再校验。新增离线重放入口，后续格式排查不继续请求源；无效候选不发布快照。
- 结果：真实300只（沪189/深111）快照与原文件哈希一致；隔离Flask目录300股票+27ETF通过，无行情仍返回DATA_NOT_READY。Python278/前端52、静态/构建及笔记本通过；默认50池不变，无推送/部署。本期MVP仍有六类门槛，见 [CR-012报告](docs/cr012-universe-validation-2026-09-08.md)。

### CR-001 补建SDD文档

- 日期：2026-09-07；批准：用户明确要求补建四文档。
- 范围：四份根目录文档及README入口；保留旧文档。
- 非目标：不改业务代码、数据库、依赖、部署，不执行新一轮上游测试。
- 关联：T-001；全部需求的基线整理。
- 验证：见task的T-001交付记录。
- 回滚：撤销四文档和README入口，不触碰业务数据。

### CR-002 明确沪深300阶段范围

- 日期：2026-09-07；用户已确认：前期沪深300相关数据，海外股票后置。
- 工作解释：成分股作为可选池、指数作为基准；属于当前设计方向，不是已实现能力。
- 待确认：首发完整300只还是明确标注的子集；固定池或历史成分回测；首页统计范围；公开展示及数据授权。
- 影响：REQ-01至REQ-08，资产列表、基准身份、概览口径、历史数据和测试。
- 实施门槛：T-002/T-003明确结论后开展T-004至T-007，不静默删除旧ETF或缓存。
- 验证/回滚：尚未实施；按design迁移和验收章节执行。

### CR-003 按所需接口验证东方财富与备用源

- 日期：2026-09-07；批准：先补文档，再分别验证东方财富指数日线与成分股历史行情。
- 范围：T-002只读、限时、低频探测，保留腾讯链路作对照。
- 非目标：不由全市场列表失败推定所有东方财富接口失败；不预先决定全面切新浪，不购买数据或更换服务器。
- 选型：待验证。新浪仅小样本成功；东方财富相关日线尚无本轮专项结论。
- 验证记录：待T-002补充接口、SDK版本、时间/环境、参数、日期/单位及限制。
- 首轮证据已补：[2026-09-07验证报告](docs/hs300-source-validation-2026-09-07.md)。两环境各两轮，东方财富历史曾成功但复测失败；中证名单可达，腾讯返回存在单位风险。T-002未整体完成，未切生产源。
- 回滚：只读探测不写缓存；后续适配器实施另登记变更和迁移方案。

### CR-004 腾讯成交量标准化风险

- 2026-09-07调查发现；正式修复待处理，关联T-002a、REQ-02/09、D-05。
- SDK把sz000前缀排除在手转股逻辑外；000001样本金钱/数量比例提示100倍差异，详见CR-003报告。
- 下一步核对原始量和资产身份，项目内修复并测试，审计旧缓存后再设计备份/重建/回滚。
- 本轮不改site-packages或生产缓存；未证明所有策略结果受影响，不能盲乘全部数据。

#### 2026-09-08实施范围

- 用户授权继续验证、测试和开发修复。先核实原始返回，再在项目适配层修复已确认的版本/代码规则，补离线测试及隔离数据联调。
- 旧缓存不能直接乘100；通过版本标记和完整重拉设计防止再次读取未校验数据，线上备份/迁移/部署另行执行。
- 当前目标是给出可复现的真实数据到分析/API链路，不静默替换全市场首页、宣称全300只验收或发布Pages。
- 新增AGENTS.md落实全项目SDD约束；本轮结果结束时回写task和验证报告。

### CR-005 模拟配置完整性与可复现联调

- 2026-09-08；关联REQ-08/09、T-007a。继续验证中确认自然日锚点、缺失数据伪中性、全退出现金缺口；在用户本轮修复授权范围内处理。
- 使用上海日期之前最近交易日作为保守数据截止日，目标为其下一交易日；不使用当天未完成数据。缺失/过期/无效信号明确失败，全退出保留100%现金。
- 修正固定行情测试未冻结日期的时钟依赖；新增临时数据库真实源→缓存→API/算法验证工具，区分进程内联调和公网浏览器验收。
- 不更改资产池、首页范围、基准身份，不推送或部署；补测试后记录结果。回滚仅撤销本批代码及文档，无线上数据写入。

## 本轮交付证据（2026-09-08）

CR-004/005本地实现与隔离研究链路已验证；详见 [修复与联调报告](docs/research-chain-validation-2026-09-08.md)。腾讯原始单位两环境一致，215项离线测试、前端12项及构建通过。云端五只未标记缓存仍待备份重拉，未推送/部署；整体T-002、沪深300新范围和MVP验收未完成。

### CR-006 安全缓存修复与云端研究回归

- 2026-09-08用户授权继续实现并尝试云端部署。先完成已验证CR-004/005的可回滚发布，不将尚未确认的300只/首页范围混入本批。
- 新增离线可测迁移工具：SQLite一致性备份、隔离副本完整重拉受影响代码，全部日期/价格/量额通过后，在维护窗口用单事务写回；失败不触碰原行情。并发修订变化拒绝应用。
- 记录原镜像与数据库备份；只更新后端，保留云前端代理及旧ETF，不创建付费资源、不发布GitHub Pages。云端验收包含真实HTTP异步回测执行及结果、相关性、排行、配置和概览降级，不只health。
- 关联T-013、REQ-02/05至10。提交/发布版本按实际记录；完整沪深300、HTTPS/浏览器仍独立验收。
- 已完成本批部署：业务提交a9c1b95，220项离线回归；五只共1335条重拉并原子应用、其他资产对照无变化。云HTTP异步回测及研究模块通过，公网health200、overview503。备份、第一次失败恢复、实际镜像与后续边界见 [云部署记录](docs/cloud-deployment-2026-09-08.md)。没有推送GitHub或发布Pages。

### CR-007 从接口回归转向完整MVP交付

- 2026-09-08；用户提供回测参数缺失、相关性503与概览空白截图，要求分步骤规划完整MVP并回写文档。
- 本轮范围：核对当前代码/契约、复核空参数校验，形成有依赖和验收门槛的实施计划；不把规划当实现，不变更线上服务或沿用上一轮发布授权。
- 已确认：use-backtest提交parameters为空且benchmark硬编码000300；common.defaultDateRange取当前日期，未接入已发布截止；App未将dataStatus传给相关性/回测。两种策略的空参数均被现有Schema拒绝。
- 日期超出已发布数据是相关性失败的重要候选原因；本轮没有该次请求的服务端追踪，不能断言是唯一原因或宣称上游恢复/再次故障。概览503是上一轮已记录的未完成能力。
- 登记遗漏的前端基准身份风险及浏览器验收缺口；保留旧HTTP测试通过事实，但只表示测试脚本对应请求通过，不代表页面默认路径可用。
- 关联REQ-01至10、T-014至T-020；详见 [完整MVP实施计划](docs/mvp-implementation-plan.md)。先P0页面闭环，再目标数据/概览，最后运维及Pages浏览器验收；需求决策只阻塞相关阶段。

### CR-008 新会话交接

- 2026-09-08；用户要求另写交接文档，使新会话可接续现有工作。
- 范围：新增docs/session-handoff-2026-09-08.md，记录版本、未提交修改、证据边界、下一批T-014入口、操作限制及文档索引；同步任务记录，不改变需求或业务实现。
- 验收：交接中的本地路径与文档链接有效，版本与工作树一致，明确云端状态为历史观测；不包含密钥内容，不把旧部署授权传递为新会话默认权限。

## 后续记录模板

```text
CR-xxx 标题 / 日期 / 状态
动机与批准范围：
关联REQ / 设计决策D / 任务T：
修改文件与契约影响：
实际验证命令、环境、结果、证据位置：
限制及未完成事项：
迁移、发布和回滚方式：
提交或PR：仅填写真实创建的引用
```

### CR-009 M0/T-014 页面默认路径实施

- 2026-09-08；当次用户授权继续实现、测试并回写文档，不推送、不部署、不修改线上数据。
- 已完整阅读交接、四份SDD、MVP计划与关联契约；WSL核实HEAD b4634d1，原有四份SDD和两份新文档全部保留。Windows Git所报两个Python文件仅为模式差异，WSL无业务修改。
- 范围：服务端Schema驱动参数与关系约束、共享已发布截止及前后端日期错误、明确ETF/无基准、任务恢复/并发与卸载保护；本地离线及浏览器分别验收。
- M0先使用全局已发布截止，明确它不保证每资产覆盖；每资产完整性仍由provider严格验证，M1进一步发布覆盖目录。日期越界返回明确400 DATE_OUT_OF_RANGE；无发布状态返回503 DATA_NOT_READY；范围内源失败保留503 UPSTREAM_UNAVAILABLE。
- 不扩展资产池、不修改首页统计语义、不实现独立指数或沿用旧发布授权；本轮结果及限制见task追加记录。
- CR-009审阅补记：analytics契约允许相关矩阵null，但charts.correlationOption直接toFixed会崩溃。纳入本批已有相关性错误体验修复：空值留空并提示，不转0；补纯图表回归和浏览器故障注入。

- CR-009图表审阅补记：真实ETF回测截图显示策略资金（约100000）与基准净值（约1）共用纵轴，基准被压扁。按现有REQ-03/06可解释比较要求，前端两序列各以首日归一为1并明确标注，不改API原始结果/指标；补比例及无基准测试。

### CR-009交付结果

M0/T-014完成（限定本地默认路径）：Python240项离线、前端32项、Ruff和构建通过；三个ETF各242条真实样本，浏览器9场景通过。参数/日期/基准/任务恢复及图表边界已实现，契约同步0.3.0；无提交、推送、部署或线上数据修改。命令、任务ID、失败记录与限制见 [M0验收报告](docs/m0-validation-2026-09-08.md)。M1需求确认及逐资产覆盖、M2—M6仍未完成。

### CR-011 M1范围确认与完整目录基础（2026-09-08，本批完成；M1整体进行中）

- 用户本轮确认：完整300只当前成分、保留ETF、固定名单最近一年回测并披露幸存者偏差；首页沪深300日频概览，团队内部教学演示。数据许可仍需验证，海外股票不纳入本期。
- 本批关联REQ-01/03/09、T-003/004/015：先设计版本化名单、独立指数和逐资产覆盖边界；实现完整目录分页与身份元数据，消除前端50条及后端成员校验100条截断。
- 验收：离线合成300只股票加ETF可完整分页检索、尾部资产不被误拒；旧total仍表示本页数量；前端拒绝混版、重复和不完整分页；接口/实现/调用方/测试同步。
- 不将测试目录当真实沪深300，不变更默认50只池或线上数据。真实名单、行情、指数及覆盖发布在M2单独验证；不推送、不部署。
- 实际结果：API0.4.0目录/身份元数据、完整成员校验和前端有界一致分页完成；Python248/前端52、静态/构建及本地浏览器13场景通过。真实外部源、300只行情和云端均未在本轮验收，见 [CR-011报告](docs/cr011-validation-2026-09-08.md)。

### CR-010 跨会话复核与模拟配置页面边界

- 2026-09-08；用户要求查看另一会话更新并继续。核实另一会话已空闲、同一工作区M0修改未提交；保留全部现有修改，不推送或部署。
- 复跑M0基线：Python240 passed/8 deselected、前端32 passed。M1范围已向用户询问，确认前不调整资产池、首页或数据使用范围。
- 在既有REQ-08/09范围继续T-018a：模拟配置尚未校验现金输入/资产数量，空现金被Number转换为0；重复调用会清掉已有状态，卸载晚响应未保护；图表把合法空持仓全现金结果视作未生成，明细不显示现金。
- 先补失败回归，再小补丁实现前端校验、请求快照与生命周期保护、现金展示；保持allocation现有契约和后端算法。分开记录离线、构建与浏览器证据，不据此宣称M4全通过。
- 本批完成T-018a本地边界：Python240/前端39、静态/构建通过；浏览器13场景和现金图视觉复核通过。详见 [CR-010报告](docs/cr010-followup-2026-09-08.md)，M1待确认，未提交/发布。

## CR-017 / T-022 第一批：旧池跨日研究保护（2026-09-10，进行中）

- 用户要求按CR-016及第9节持续本地开发并尽量推进MVP；保留原六份Markdown改动，基线3d8bc3f。不沿用发布授权。
- 范围：配置以全局已发布截止为basis，过期/未来/更新中/失败明确拒绝；研究只读缓存，计算期间日期/目录/修订变化拒绝；回测另存提交时上下文并在执行时核对；响应与页面显示截止/名单/修订。
- legacy_revision只表示旧缓存修订，不是完整发布快照。旧缓存含晚于发布截止的行时拒绝该资产，避免新前复权基准混入；更新中/失败拒绝旧可变库研究。T-016j接续不可变批次及失败保留旧发布。
- 验收：固定09-09时钟+全局09-07+局部09-08、周末节假日、未知发布/部分失败、缓存缺口、计算中更新、入队后跨版和旧任务查询；接口0.5.0同步契约/实现/前端/测试。
- 本批不扩采、不切300池、不访问云/发布/修改线上；完整T-022与MVP不据此完成。结束时补实际命令、结果和限制。

CR-017首批本地回写：336项Python离线/53项前端通过，WSL构建及Ruff通过；研究只读、配置截止/过期、版本冲突及回测上下文已实现。详见 [验证记录](docs/cr017-research-publication-2026-09-10.md)。完整不可变批次、300池和MVP仍未完成，无新联网/浏览器/云发布证据。

## CR-018 / T-016f 停牌覆盖及研究语义（2026-09-10，进行中）
复用CR-013留存观察与报告中已核实官方688981停牌公告，新增证据化非交易事件；区分已解释停牌/上市前/代码身份中断和未知漏数，保留300分母。固定名单最近一年不新增历史成分重建。相关性只计算相邻共同有效收盘区间并披露共同日期规则；回测停牌不成交，下一实际开盘执行，资产估值可延续最后成交收盘但只用于净值，不补写OHLCV；开头无成交保留分配现金，整段无行情且无法建立估值明确拒绝。离线真实688981重放+未知缺口/冲突证据/多资产初始现金与复牌成交测试。六只限额本批不变，不联网/发布。

CR-018本地回写：346项Python离线、53项前端、WSL构建及静态通过；688981真实样本零网络重放241条+1个证据停牌，完整300分母仍294未采集。详见 [验收记录](docs/cr018-trading-events-2026-09-10.md)。无云操作/发布。

## CR-019 / T-016h 有界扩采协调器（2026-09-10，进行中）
在旧prepare_history_batch最多6只门禁之外新增协调器；单worker仍最多5次HTTPS/40秒，串行最小1秒；持久预算预留、断点恢复、幂等观察、互斥和隔离候选。首轮仅30只，最多150次实际HTTP、总时长1200秒，无自动重试或换源。目标区间2025-09-09至2026-09-09，名单固定CR-012源日09-07，明确非重新验证今日成分。先离线预算/中断/篡改/锁/恢复测试再真实样本；所有300都有条目，未知不发布。首批完成后才决定扩300（另记预算）；当前不切生产、不采集概览、不更改线上。

CR-019启动故障回写：首次从stdin调用仅修改父进程sys.path，子进程未继承PYTHONPATH；只读find_spec确认quant_platform不可见，已停止所有采集进程。原ledger保留25次尝试/125请求预留，HTTP实际未获得证据，不伪记为源不可达或确定零请求。修复显式子进程环境并加无网络启动预检、连续3次失败熔断；新r2阶段预算另限150请求/1200秒，先测试再验证，不重写原证据。

CR-019阶段门槛：r2已取得30只各243条、共7290条，全部complete，实际90次HTTP、预留150、采集计时66.13秒；仍270未采集。下一步同名单/事件/区间在新目录扩300，新增预算最多1350次HTTP/3600秒；先验证种子复用哈希/配置一致性，30只成功观察不重复请求，其余270仍串行、单只5次/40秒、连续3失败熔断。禁止自动发布；未知缺口保留待分类。

## CR-020 / T-016i 独立沪深300指数（2026-09-10，进行中）
已只读检查AkShare1.18.94指数专用stock_zh_index_daily_tx，腾讯明确带市场身份sh000300，不使用股票000300映射或510300ETF替代。先仅最多2次已核实腾讯HTTPS请求、单次10秒读取/5秒连接、总45秒、响应各最多2MiB、无重试/重定向，留存原始响应及哈希到新artifacts；验证明确指数键/名称和2025-09-09至2026-09-09价格指数日线。只使用价格点数，成交量语义未验证则不作为股数或成交额。成功后实现显式指数基准身份、离线存储读取、同期收益/Alpha/Beta与调用方契约；失败保留证据，不偷偷替换ETF。未运行部分不标完成，无云修改或发布。

## CR-021 / T-022、T-018 图表体验收尾（2026-09-10，登记）

范围限于既有截图问题：无有效基准不生成图例/序列，净值tooltip固定4位且不改变序列精度，相关性色条去掉可拖动手柄并保留-1至1，桌面/移动端锚点避开导航。验收：图表单测含空基准、零值、非有限值、原始精度；前端构建和本地浏览器检查，分别记录，不能代替300池联调。

## CR-022 / T-016f、h 全量例外证据与离线重分类（2026-09-10，登记）

300只原始采集完成，289完整、11缺口。依据本轮读取的交易所/巨潮公告登记明确的停牌、上市前区间；允许港交所披露易中发行人明确A股代码的公告，仍验证资产身份，不以H股停牌替代A股。无法读到一级证据的缺口保持unknown。新增显式offline-reclassify模式：必须有校验通过的源候选，只允许同名单同区间以新事件版本重算，复用包括gaps在内的有效原始观察；禁止HTTP，未采集资产仍未采集，写新候选且保留seedCandidateId；不自动发布。验收含篡改/缺口/零网络测试及真实300离线重放。

CR-021本地结果：前端54测试/构建通过；实际Edge真实三ETF副本+worker、无基准/过期配置/1440、820、390锚点及溢出通过，详见[报告](docs/cr021-browser-validation-2026-09-10.md)。CR-022首轮300离线重算289+9例外+2未知、72746条、0HTTP，契约/哈希通过；本轮已补齐最后2只官方公告，新的重算待验，详见[报告](docs/cr022-coverage-reclassification-2026-09-10.md)。完整发布/概览/全量联调/两个实际交易日仍不得勾选。

## CR-023 / T-016h ETF独立候选（2026-09-10，登记）
保留旧27ETF完整身份和入口，按同一年区间单独串行验证腾讯行情，不将ETF加入股票300分母。仅probe白名单内ETF可进入已验证腾讯路径；旧股票校验保留。总上限135请求/1200秒、单只5次/40秒、1秒间隔，3次源失败停止；新隔离目录，先预留后请求，中断保留不自动重试。每只质量/哈希/来源记录，未知缺口阻止327完整发布，不静默删除跨境ETF或取消交易日检查。先离线测试再实采；没有云端部署或线上修改。

## CR-024 / T-016j、T-017 本地完整发布与概览（2026-09-10，登记）

本地发布输入为同名单同区间的300股票候选、独立27ETF候选、独立CSI300指数与证据事件；逐条重验身份/哈希/交易日覆盖，不信任complete布尔值。发布包内容寻址，先完整构建校验再原子替换current.json；失败保留旧指针。服务启动一次读入验证后的不可变内存快照，运行中旧请求/任务不随指针变化混版；新进程载入新批次，旧排队任务版本不符明确失败、旧完成结果保留。暂不声称无重启热切换或跨批次旧任务重算。

新增GET /data/coverage返回327条覆盖及dataContext、成员分母300/ETF27；旧可变运行返回503 DATA_NOT_READY。研究context一致性为published_snapshot，日期采用包范围。概览同包仅统计当前300成分相邻交易日复权收盘变化，区分停牌、涨跌平；成交额来自同日成分总额，缺失不补0；价格指数单独显示，北向/涨跌停保持null并从新UI移除北向卡片。显示当前名单/幸存者偏差。OpenAPI同步0.7.0。

验收：缺失/错配/篡改/失败保留旧包、旧reader固定/新reader切换、API覆盖/日期/上下文、真实327本地五模块及浏览器；离线与真实分别记录。只操作artifacts本地发布目录，无云端操作。连续两个实际交易日与HTTPS仍独立待验。

CR-023/024结果回写：27ETF6561条/81HTTP全完整；真实327本地不可变发布与五模块联调通过，边界3测试、前端54测试/构建已通过。详见[ETF报告](docs/cr023-etf-validation-2026-09-10.md)及[发布报告](docs/cr024-publication-validation-2026-09-10.md)。本轮完整Python回归及扩展UI恢复/故障场景仍待执行，审批连接中断不是测试通过；连续两个真实交易日尚未验收。

## CR-025 / T-019 连续实际交易日自动更新（2026-09-10，登记）
使用本任务每日07:30（Asia/Shanghai）自动跟进，处理前一自然日之前最近已完成交易日；因此首个计划处理2026-09-10行情的运行在09-11早晨，第二个处理09-11行情的运行在09-12早晨。不是伪造两个交易日当轮通过。保留固定当前300名单的既定研究范围，27ETF与指数逐批更新，最近一年整段刷新避免跨日qfq修订混合。

自动验收程序只写artifacts/cr025-daily-20260910及其独立publication；初始旧完整包留作失败回退。每个交易日仅一次尝试，不自动重试失败日，最多4个实际交易日尝试；每次股票1500/3600秒、ETF135/1200秒、指数2/45秒，总上限1637请求/5400秒，最多6548请求。先候选完整校验、五模块程序检查，再原子发布；失败记录分阶段错误，旧指针保持。相邻成功交易日才计连续2天，手动运行/dry-run不能计自动验收，最终还须核对调度器实际触发记录。

本地程序互斥、日期/预算/重复日门禁、真实触发时间/目标交易日/版本/结果持久记录；机器与桌面app需在运行，审批/网络失败只记失败。成功两天后停止自动采集并回写报告；四次耗尽则停止等待新授权。先离线测试及零网络预检，再启用任务，不新增云部署权限。


2026-09-10最终批次回写：CR-023 27ETF/6561条完整；CR-024 300股票+27ETF+独立CSI300指数本地发布、概览和完整五模块联调通过。实际Edge十资产、无基准/指数基准、刷新恢复无重复提交、模拟配置、503禁用、三屏宽均通过；完整离线回归375 passed/8网络deselected，git diff --check通过。修正新增coverage接口清单测试及旧契约说明，未删除检查。详见[CR-023](docs/cr023-etf-validation-2026-09-10.md)、[CR-024](docs/cr024-publication-validation-2026-09-10.md)。CR-025程序及门禁就绪，自动化300-mvp已ACTIVE，每日上海07:30；当前实际自动成功日0，不能勾选连续两日/MVP全验收。详见[日更验收](docs/cr025-daily-acceptance-2026-09-10.md)。不涉及云推送/部署、收费、旧库迁移；HTTPS及许可仍待独立验收。

CR-024视觉复核补丁登记：结果标签按dataContext.consistency显示完整不可变发布/旧池修订，修正新包误标旧池，不改变计算或版本；验收为前端测试、构建及真实浏览器标签断言。

CR-024视觉补丁结果：标签显示完整不可变发布，前端54测试和构建复验通过；完整浏览器场景及动画完成截图重跑通过，十资产矩阵、指数净值和移动端图已人工复核。最后Python结果375通过/8网络排除不变。


## CR-026 / T-020 HTTPS 与云端最终验收（2026-09-10，登记）
用户当次要求继续HTTPS与云端最终验收，授权在既有云主机推进本批部署/验证；不购买域名或付费服务，不公开再分发原始行情，不覆盖旧数据卷或强推Git。先核对现有主机身份、云版本、域名/DNS及Pages控制权限，再准备可审阅发布物。当前已严格SSH核实云HEAD af02f826、两旧容器healthy，仅HTTP80/API127.0.0.1:8000，无443监听；域名未配置，已向用户索取域名/DNS信息。

范围：将CR-024不可变327资产包以只读方式装配到隔离云候选，保留旧服务/镜像/数据库，启用前备份与哈希核验；精确Pages Origin和X-Research-Version预检、请求体/速率/并发保护、可信HTTPS及证书续期配置。优先候选本机端口验证，再切正式入口；域名、证书或GitHub控制信息缺失时完成不依赖它们的准备，不自造域名或关闭TLS校验。

验收分层：配置/部署门禁离线测试、云候选五模块同一版本、真实证书链/域名/有效期、Pages浏览器跨域五模块及恢复、无混合内容、故障/限额/回退证据。证书curl或候选HTTP不能代替真实Pages HTTPS浏览器。M5实际两日日更保持独立调度，不因本批云候选通过而勾选。逐批回写命令、版本、备份与未完成门槛。

CR-026路径补充：官方2026-03-11说明已核实Certbot>=5.4支持公网IP证书，现有43.161.223.91可作后端HTTPS标识，无需购买域名；以此继续准备，用户提供域名时再调整。六天短证书须有每日续期检查/成功reload及真实链验证；staging证书只作签发预检，不计正式HTTPS。保留旧HTTP服务，仅增加ACME挑战路径，候选在独立目录/容器/只读发布和独立任务副本上验证后开放443；精确Pages CORS与资源限额一并验收。

CR-026阶段回写：只读云身份/版本/健康与Pages旧版本已核实；隔离发布包2305993字节/66文件和部署门禁已准备，2项离线检查与bash语法通过。SCP上传因自动审批要求明确授权源码+327行情快照至43.161.223.91而被拒绝，未执行云部署或TLS签发。详见[报告](docs/cr026-https-cloud-validation-2026-09-10.md)，HTTPS与云最终验收未勾选。

CR-026续作授权：用户明确回复“是，继续上传然后链接给我看看效果”，允许已列明2.3MB源码/前端产物/327快照上传43.161.223.91并安装隔离目录。本轮继续固定哈希包，先备份/候选验证，再HTTPS与可访问链接；旧卷不覆盖。

CR-026云候选联调发现代理在新旧Docker网络共存时使用backend通用别名，错误命中旧50池；修正为唯一候选容器名quant-mvp-cr026-backend-1，再重验327/版本/概览。原始包保留，补丁单独记录，healthy不代替数据门禁。

CR-026交付补充：本机正式TLS/327状态已通过但公网443超时，已请用户在云控制台放行。为完成用户“链接看看效果”，在旧80入口备份配置后代理到新候选提供明确临时HTTP预览；旧镜像/卷/后端保留且可恢复原路由，不把HTTP预览计HTTPS通过。完成证书每日续期检查和reload，再继续公网浏览器。

CR-026公网浏览器发现图表大JS偶发ERR_CONTENT_LENGTH_MISMATCH，直接下载字节完整；不臆断根因。新增HTTP代理改流式传输并为JS/CSS启用压缩，源文件/hash不变，继续真实浏览器验收。

CR-026上传续作回写：用户明确授权后固定包已上传/校验并启动云隔离新327包，HTTP新预览为http://43.161.223.91/；两库备份完整，9个旧完成任务保留，公网五模块API通过。正式IP证书与本机TLS、自动续期dry-run已通过；公网443仍超时，需云控制台放行。普通浏览器加载成功但完整脚本仍有资源传输失败，未勾选浏览器/Pages/最终HTTPS。详见[实际结果](docs/cr026-https-cloud-validation-2026-09-10.md)。旧准备阻断记录已被本次用户授权解除，不再索取同一上传授权。


## CR-027 MVP核对与GitHub Pages闭环（2026-09-11，登记）
用户本轮明确要求检查MVP并按既定云后端+GitHub Pages前端部署，授权对应代码推送与Pages发布。范围为既有五模块、前端启动故障修正、固定HTTPS API配置及跨域验收；不上传真实行情/数据库/密钥到GitHub，不强推、不购买服务。已核实图表动态导入拒绝会阻止initialise；公网443超时，现有Git SSH认证失败，两者分别处理。CR-025今日候选失败，不自动重试同日，不认定MVP全部完成。


CR-027本轮回写：前端图表/API启动隔离已实现；57项前端测试、Pages独立构建、本地Edge正常/图表失败两场景通过。云本机可信TLS/327状态/Pages CORS/续期状态通过，公网443超时；仓库专用SSH读权限已核实，早先默认身份失败不再作为Git阻塞。候选未发布，Pages及MVP全验收未通过；全Python和diff检查因自动审批通信中断未执行成功。CR025首日失败1/4，606HTTP/201可解释覆盖/3源错误/96未采集，不重试同日。详见[CR027审计](docs/cr027-mvp-pages-validation-2026-09-11.md)。


CR-027最终补验：审批通信恢复后git diff --check通过，完整Python离线377 passed/8 deselected（39.86s），2条既有network标记未注册警告；前端57通过及两浏览器场景不变。本地验证服务已停止。此前审批中断记录保留为历史，已不再阻塞本批验证；Pages候选未发布，当前等待公网TCP443放行，未认定Pages数据闭环或MVP完成。


CR-027发布续作（2026-09-11）：用户已放行443并要求继续，公网curl严格证书验证成功，/api/data/status返回327/09-09/stale及既有e5f794c7094c版本。解除TLS连接阻塞，继续已授权的源码推送与GitHub Pages发布。保留过期快照边界，不将新页面发布当作日更完成。
