# 技术设计

更新日期：2026-09-08。关联：[PRD.md](PRD.md)、[spec.md](spec.md)、[task.md](task.md)。当前实现与目标设计分开记录。

## 当前实现

```text
Vue / Vite / ECharts → Flask API / 契约 / CORS
    → Python算法 → 行情SQLite / 概览快照
    → 回测任务SQLite → 后台worker → 持久化结果
数据更新 → AkShare / 上游子进程 → 校验 → 缓存和分项状态
```

| 位置 | 当前职责及限制 |
| --- | --- |
| [前端](apps/frontend/src) | 五类页面、API客户端、分项错误提示；不直接访问行情源 |
| [路由](services/backend/src/app/api/routes.py) | API0.4.0资产目录分页已实现，默认50/单页100；历史/概况/研究路由保留 |
| [行情适配](services/algorithms/src/quant_platform/data/akshare_provider.py) | 默认50只混合池；显式快照可加载300股票+27ETF，尚未切生产；历史主路径为腾讯 |
| [概览](services/algorithms/src/quant_platform/data/market_fetch.py) | 东方财富全市场分页核心及可选指数阶段，不是沪深300成分概览 |
| [上游执行](services/algorithms/src/quant_platform/data/upstream.py) | 子进程限时；历史40秒、概览核心180秒等预算详见故障记录 |
| [存储](services/algorithms/src/quant_platform/data/storage.py) | SQLite、校验、修订号；概览保留最后成功快照 |
| [日历](services/algorithms/src/quant_platform/data/calendar.py) | 2025—2026；超范围报错，停牌/上市缺口当前保守拒绝 |
| [排行](services/backend/src/app/services/ranking.py) | 按修订失效、有界缓存；计算预算不是CPU进程强制终止 |
| [部署](compose.regression.yml) | 后端回环端口、持久卷、维护更新及临时前端代理，不等于最终Pages验收 |

细节见 [既有架构](docs/architecture.md) 和 [数据故障处理](deploy/regression/data-resilience.md)。旧部署描述是历史记录，不覆盖spec中的新观测。

## 设计决策

CR-012实施前设计：新增不可变名单快照模块，校验官方指数000300、同一源日期、300个唯一六位股票/市场身份，未知或矛盾交易所、空名称和未来日期拒绝。原始来源文件与规范化JSON绑定哈希，读取时重新校验，不能仅信任手改JSON。sourceDate只表示源文件日期，不臆造调样生效日期；固定当前池明确幸存者偏差。仅构造器显式传入快照时组装300股票+旧ETF，默认50池不变，存储不迁移；后续发布需完整行情覆盖及独立授权。下载工具只写新建artifacts目录，非生产发布器，失败保留诊断而不切换默认池。

| 编号 | 决策 | 状态 |
| --- | --- | --- |
| D-01 | 保留前后端分离，最终Pages前端+云后端 | 既有方向 |
| D-02 | 沪深300阶段日频研究，不依赖全市场实时列表完成核心分析 | 已确认范围；新概览待实现 |
| D-03 | 按成分/股票历史/指数历史/概览分别选源；先验证东方财富所需日线 | 验证已获同意，最终源未定 |
| D-04 | 名单和行情解耦；指数、股票、ETF不因相同六位代码而混用 | 当前名单快照已实现；独立指数/覆盖发布待实现 |
| D-05 | 失败保留旧数据并标状态，不静默拼接异源复权序列 | 既有原则，跨源迁移待实现 |
| D-06 | 初期保留SQLite，先验证300只规模，不直接升级机器/数据库 | 拟议，性能待测 |

## 目标数据边界（未实现部分）

### D-03首轮验证更新

2026-09-07 [两环境两轮验证](docs/hs300-source-validation-2026-09-07.md)：中证名单300只可达；SDK指数前置列表失败，历史直连曾成功但复测失败，不可直接升为稳定主源。腾讯两只历史可达，但sz000个股成交量单位有风险，先完成T-002a。最终源未定，生产provider未变。

新增 [只读探测工具](tools/check_hs300_sources.py) 和 [离线安全测试](tools/test_check_hs300_sources.py)。固定日期用于复现；进程结束和返回数据不代表完整质量验收。

### 拟议分工

2026-09-08 CR-004实施设计：对已固定AkShare版本的sz000个股原始量、SDK量及独立源作对照；确认后在项目适配层修复，不修改site-packages。旧缓存需要与修复后的规范版本隔离或有可验证的迁移标记；无标记的受影响缓存不能当正确数据命中。修复先经隔离缓存和真实API/算法调用验证，再安排生产数据迁移。

1. 成分提供器保留来源、快照/生效日期和成员，历史调样单独标注；当前名单不能代替历史名单。
2. 股票历史提供器输入资产身份、区间、复权，输出规范日线及来源。
3. 指数提供器单独处理指数身份；不能用当前股票代码前缀推断把000300当作深市个股。
4. 概览构建器基于完整同日的目标范围计算；Q-03批准前不替换现有全市场语义。
5. 更新协调器执行采集、暂存、校验、发布；页面只读已发布结果，不在每次页面请求切源。

拟议规范字段包括类型/市场/代码、交易日期、OHLC、成交量、成交额、复权、来源、采集时间、名单版本及数据修订；尚未全部存在于数据库/API，须在T-004设计迁移。

统一目标为成交量股、成交额人民币元；实际源手/股等差异按版本确认转换。缺失成交额保留缺失，不能用价格乘成交量代替。展示涨跌与复权研究收益分别定义。

## 契约和迁移

- [OpenAPI](packages/contracts/openapi.yaml)仍生效，本次不新增虚构路由。
- 300只检索设计分页或经验证的上限调整，联动路由、Schema、前端和边界测试。
- 基准类型/市场歧义通过T-004设计解决，不直接假定现有benchmark能正确处理指数。
- 概览/状态新增范围、覆盖和来源字段时同步契约，不能仅把300只结果装进旧全市场标签。
- 换源前备份SQLite与快照，隔离验证数据，对比重叠区间再迁移；不覆盖后才检查单位。
- 迁移可重复、重启可读，不删除旧ETF历史，不承诺无需代码修改即可换源。

## 数据验证

CR-014云只读复测：通过stdin在现有后端容器内存加载已测预算/覆盖助手，调用已部署provider的无存储日线函数；不替换文件或构造缓存。实际六只18次HTTPS成功、云/本地记录一致。旧概览缓存仍503，没有据此断言东方财富当前连接失败。诊断设计和证据见 [报告](docs/cr014-cloud-history-2026-09-08.md)，业务与HTTP契约不变。

CR-013实施前设计：新增纯覆盖评估器和磁盘coverage.yaml（不改变HTTP0.4.0）。候选批次绑定universe snapshotId、区间、SDK/来源/单位及每资产观察文件SHA256；全部名单均有条目。外部采集在独立短生命周期worker内对requests设置域名、次数、超时、响应大小限制，结束恢复补丁；父进程40秒硬截止。每资产观察单独留存，只有基本质量及全区间交易日通过才写新隔离库；不调用update_daily或市场概览，不改全局发布状态。SDK返回中重复日期在项目适配层拒绝，避免后续drop_duplicates掩盖冲突。批次失败保留观察，重放只读观察后写另一新目录；不续写旧批次、不合并异源/异复权基准。批次manifest原子替换临时文件仅针对新隔离目录，不是生产发布器；存储revision不能冒充正式dataVersion。

CR-013落地：上述工具、磁盘契约及39项新增离线测试完成，全Python317通过；本地6只18次真实请求，5只完整、1只gaps，零网络重放和只读库逐值校验通过。原始HTTP字节未保存，证据为SDK规范化观察；不得夸大原始源审计能力。688981缺日经官方公告核实为停牌，后续新增证据版本与可交易日规则，不放宽当前研究完整性门禁。详见 [报告](docs/cr013-history-validation-2026-09-08.md)。

CR-012落地：universe.py及磁盘Schema版本1、限次下载/离线重放、显式provider与应用统一注入；精确映射官方“成份券”/“成分券”和中英表头，拒绝重复歧义。真实源文件日期不是调样生效日期，current_snapshot固定披露幸存者偏差。仅在新隔离目录验证327资产接口，尚无自动运行配置、日更协调或数据迁移。实际命令/证据及六项剩余门槛见 [报告](docs/cr012-universe-validation-2026-09-08.md)。

### T-002只读验证

- 东方财富指数日线、个股历史、成分来源分别测试，记录SDK内部辅助请求，防止误认失败接口。
- 从核实名单抽少量沪/深样本，同区间同复权对照腾讯；另测除权或停牌区间。
- 限时低频、单接口硬截止，执行前说明总请求预算；不触发全市场/全历史更新，不写生产缓存。
- 记录时间/时区、环境、SDK、接口/域名、参数、TCP/HTTP/业务结果、行数、日期、单位和失败类型，不记录凭证。
- 小样本成功只允许扩大验证，不代表全量/持续稳定；异常文本不能单独确定故障责任方。

现有 [check_market_upstream.py](tools/check_market_upstream.py) 的history走腾讯、spot走当前概览，不是东方财富日线专项工具；新探测必须明确输出来源。

### 扩大范围和业务验收

- 名单唯一、范围完整，所有可选资产有明确的历史可用区间。
- OHLC合理且有限、日期有序，成交量/额非负；停牌/上市前无数据不得当普通漏数补价。
- 分别验证复权切换、除权前后、异源重叠、指数与ETF区别。
- 超出现有日历范围先补日历及测试，不跳过校验。
- 相关性、回测、排行、建议使用明确的数据/名单版本和成本口径。

## 模拟配置修复（CR-005）

采用保守日频截止：上海当前日期减一天后取最近交易日；目标取已验证日历的下一交易日。即使当天已收盘也不提前使用当天数据。每个资产必须覆盖截止日且至少10条；无数据、过期和空/非法信号报上游不可用，不生成中性权重。全退出时实际现金为100%；其他情况投资权重加现金为100%。接口字段不变，同步Schema描述及测试。日历覆盖外失败，不猜测假日。

## 2026-09-08实现落点

CR-004已在provider及storage实现版本限定的000个股量修复和原子缓存规范标记。云端五只旧缓存无标记，新代码直接上线会拒绝其命中，必须先做备份/隔离重拉/切换验证，不提供盲乘迁移。CR-005已实现并经API验证。建议数据分工继续为官方名单+腾讯股票历史+独立指数提供器，后者尚未接入。验证层级与生产迁移门槛详见 [报告](docs/research-chain-validation-2026-09-08.md)。

## CR-006维护迁移设计

迁移只处理已存储、未带正确版本标记的000个股/复权模式。备份目录必须新建；备份通过SQLite backup API生成，原数据库只读。副本按每段完整日期重拉，拒绝缺交易日；所有目标成功后再进入原库写事务，核对原OHLCV修订号与原记录摘要未变，仅替换目标历史、规范标记并递增修订。不整库覆盖，不清空其他资产或任务库。维护窗口停后端及项目更新写入；事务失败回滚。部署前保留原镜像及行情/任务库，失败恢复匹配版本。前端代理仅reload后端地址。

实施落点：tools/repair_volume_cache.py使用全库逻辑摘要而非仅OHLCV修订号，保护准备期间任何原库变更。2026-09-08已部署a9c1b95；五只缓存完成迁移，HTTP研究回归通过。只读挂载的停写SQLite备份需确认无未合并WAL后使用immutable只读连接；运行中的数据库不能照搬此方式。准确备份与回滚边界见 [云部署记录](docs/cloud-deployment-2026-09-08.md)。

## CR-007 页面闭环与MVP设计计划（尚未实现）

本轮代码核查发现页面固定提交空策略参数、以浏览器当天初始化日期、硬编码未适配的000300基准；不通过放宽后端校验掩盖缺陷。实施次序和验收见 [完整MVP计划](docs/mvp-implementation-plan.md)。

- 策略表单：从策略metadata的parameterSchema生成默认值、输入项及校验；策略切换重置参数，metadata未就绪时禁止提交。后端保留必填和业务约束，前端不假定JSON Schema的default会被服务器自动填充。
- 日期状态：回测/相关性共享已发布数据截止，先使用全局latestTradeDate保守初始化，再按资产历史可用区间取交集；全局日期不能证明每个资产均完整。缺状态时不回退当天，异步刷新不覆盖用户已编辑日期。后端区分请求越界、覆盖不足及真实上游失败，新增错误详情先同步契约和调用方。
- 基准身份：由支持目录明确股票/ETF/指数类型和市场身份，禁止六位代码隐式解释为另一类资产。指数适配前只提供明确的旧ETF或无基准；正式CSI300基准另做独立序列验收及收益口径说明。
- 数据发布：官方名单快照、股票历史和指数分别适配；后续契约设计加入名单/数据版本、资产可用区间及覆盖状态。概览与研究读取一致的已发布版本，合法停牌与缺失分类处理，不取消完整性检查；这些字段当前并非均已实现。
- 页面和任务：默认操作、输入错误、忙碌/失败/空结果、轮询取消、任务ID与刷新恢复均加入验收。真实浏览器使用页面生成的请求，不能由测试脚本代填参数后宣称页面成功。
- 发布门槛：M0先修旧资产池默认操作，M1—M4完成确认范围的数据及五模块，M5验收真实跨日更新/故障恢复，M6验收Pages HTTPS全链路。前端改动必须构建发布对应版本，仅重载代理不更新前端业务代码。

本次仅登记设计，未改OpenAPI或实现；范围收缩、数据许可、域名和发布依PRD待确认项处理。

## 部署和回滚

CR-015：本次用户授权现有云服务器临时预览前后端一起更新，前端使用同源/api，不改变最终Pages方向。复用现有卷与配置，不启用可选universe_snapshot。先保存旧两镜像标签/代码和SQLite一致性备份，再构建切换；健康/回归失败回退匹配旧镜像。行情无模式迁移，不能无条件恢复任务库而丢失新任务。公网HTTP仅教学演示，不填写敏感信息；概览失败如实保留。

最终Pages经HTTPS访问云API；云前端仅回归/代理入口。HTTPS、CORS、浏览器联调、调度、备份恢复分别验收。

流程：小补丁和离线回归 → 只读源验证 → 隔离数据验证 → 经授权部署 → 严格烟测及浏览器五模块回归。当前严格烟测基于旧概览语义，范围变更批准后同步更新契约、实现和测试，不仅删检查项。

保留旧代码和匹配的数据备份，回滚时同时检查Schema/来源/复权兼容性。删除数据卷、退款、换主机和开通收费服务另需授权。

### CR-009 M0实现方案（实施前登记）

- 参数Schema增加x-relations（left、operator=lt、right）表达两策略字段关系；前端单一表单解析器负责默认值与数值校验，后端继续执行原有Schema和策略业务校验。
- 共用useResearchDates读取dataStatus.latestTradeDate，只初始化未编辑日期；校验2025—2026现有日历及发布截止。后端共享ResearchDateGuard，相关性计算/回测入队前验证；全局状态与逐资产完整性分开，不读取真实库进行迁移。
- 基准默认省略；可选项使用已成功加载的资产目录中ETF，服务端同步拒绝不支持的基准。BacktestJob新增request快照，恢复后展示提交时的策略/参数/基准，避免当前表单误标旧结果。
- 本地保存最近任务ID；有界轮询、显式恢复、代次令牌和组件卸载保护；超时保留ID，不自动重新提交。离线测试模拟边界，真实浏览器使用页面按钮及实际服务端worker；真实行情与测试fixture证据分别记录。
- 回滚仅撤销本批代码与契约，无存储迁移；四份SDD、原有交接文档保留历史记录。具体命令、实际结果与未完成项结束时追加。
### CR-009实现与验证结果

方案已落地：共享日期与Schema表单、ResearchDateGuard、ETF校验及BacktestJob.request；无需存储迁移。补充有限数校验、不存在任务恢复、nullable矩阵及两序列首日归一；全局截止仍不保证逐资产覆盖。Python240/前端32、Ruff/构建及真实本地浏览器9场景通过，详见 [M0报告](docs/m0-validation-2026-09-08.md)。浏览器仅三ETF隔离库与真实worker，运行结束已停止本地服务；完整沪深300、日更和公网链路未验收。源码未提交或发布。

### CR-010模拟配置页面方案（实施前）

useAllocation采用与已实现研究表单一致的输入约束、可注入API客户端和作用域销毁标记；提交前同步复制symbols/strategyId/cashPct，忙碌时直接返回，不清结果或错误。数值校验拒绝空值、字符串和非有限数，不依赖HTML边界或Number强转。图表先汇总持仓与现金，再判空；页面明细单列实际现金。仅前端实现既有allocation契约，不新增路由或修改数据/存储结构。

视觉复核补记：首轮截图在饼图初始动画时抓取，仅有标签而扇区未绘制，不能作为完整视觉证据。配置权重图关闭过渡动画，零权重只保留在明细、不画空扇区；浏览器验收增加SVG圆弧实际绘制检查并重新截图。

上述实现完成，前端39项/浏览器13场景通过，最终现金图已人工复核；未改后端或契约。命令与限制见 [CR-010报告](docs/cr010-followup-2026-09-08.md)，M4整体仍待验收。
# CR-011 / M1设计增量（2026-09-08）

用户已确认完整300只当前成分、保留ETF、固定名单最近一年、沪深300日频概览及团队内部演示。最新设计见 [M1目录与迁移边界](docs/m1-catalog-design.md)。本批实现分页/身份元数据和完整成员校验；名单快照、独立指数、逐资产覆盖及原子发布为后续设计，尚未实现。旧文档的待确认说明保留作历史记录，当前范围以本节及PRD决策表为准。
# CR-011实现回写

目录分页与身份元数据按 [M1设计](docs/m1-catalog-design.md) 实现，API0.4.0同步服务/调用方/测试，默认旧池与数据库不变。Python248/前端52、本地13场景通过；[实际证据和兼容边界](docs/cr011-validation-2026-09-08.md)。后续名单/指数/覆盖发布仍需实现及真实验证，未部署。
