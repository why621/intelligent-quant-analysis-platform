# 跨会话续作与模拟配置页面验收

2026-09-08；CR-010 / T-018a；HEAD仍b4634d1。保留另一会话的全部M0未提交修改，本轮未提交、推送、部署或访问云端。

## 实际交付

读取另一会话01a07ffd-ec4e-7fe1-85fd-0201dc926b72及四份SDD、[M0报告](m0-validation-2026-09-08.md)，复跑其基线为Python240 passed/8 deselected、前端32 passed。M1范围与内部/公开用途已向用户提问，未确认前不调整资产池、首页、数据源和许可。

先完成既有REQ-08范围的T-018a：现金必须显式填写0—100有限数，空值不再转0；校验1—10只不同六位代码及可用策略；提交输入快照、忙碌时防重入、卸载忽略晚响应、错误可重试。结果明细单列实际现金；合法空持仓全现金不再显示等待。零权重留在明细而非饼图空扇区，配置图关闭动画以稳定展示。

实现文件：use-allocation.js、App.vue、charts.js；新增allocation.test.js并扩展verify_m0_browser.cjs。不改后端算法或现有allocation契约。

## 实际验证

- Python：按M0报告完整命令运行services及两个tools测试文件，240 passed、8 network deselected，14.37秒。本轮未改Python业务。
- 新测试首次7失败：含输入校验/空现金图缺陷，以及旧composable未支持客户端注入的测试接线失败；不把所有失败都当独立产品问题。Node中的相对API地址解析失败，未产生外部行情请求。
- `npm run test --workspace @intelligent-quant/frontend`：39通过；最终再次运行`node --test --test-reporter=dot apps/frontend/tests/*.test.js`通过。
- `npm run build:frontend`通过，最终主包index-KiS0gTIY.js；Ruff检查services/algorithms、services/backend、tools/serve_m0_local.py通过；`git diff --check`通过。
- 浏览器：`node tools/verify_m0_browser.cjs http://127.0.0.1:8765 artifacts/cr010-browser-final`，使用已有Playwright与Edge152.0.4191.66，13场景通过，pageErrors为空。
- 复用artifacts/m0-20260908三ETF真实缓存，不运行prepare、不重新抓行情；服务禁用上游、仅绑定127.0.0.1:8765，测试回测仅写隔离任务库。验收后Ctrl+C停止，ss确认8765无监听。

浏览器新增4场景：空/越界现金禁用、真实API现金0与100（持仓+现金均100%、basisDate09-07/targetDate09-08）、空持仓100%现金。最后一项为明确标注fixture注入，不是行情信号证据。其余9场景复验M0表单、日期、worker、恢复和故障边界；相关性241样本，3个回测各242点。

最终本地任务：均线e8c3130e-1a1c-4af3-9b8a-d327f3365409；动量8f8e73fc-4e26-467c-94d8-7b890104e6c8；ETF比较c1bb98c9-05a1-4478-a6e6-9b31dad61289。仅用于回归定位，不评价投资表现。

证据均在Git忽略目录：

- [最终浏览器请求与结果](../artifacts/cr010-browser-final/evidence.json)
- [现金100%截图](../artifacts/cr010-browser-final/allocation-100.png)
- [现金0%截图](../artifacts/cr010-browser-final/allocation-0.png)

首轮脚本13场景通过但截图落在初始动画，仅有标签无扇区；随后关闭配置图动画，补SVG实际圆弧检查并重跑。最终100%现金环与明细人工审阅正常，第一次截图不作为最终视觉证据。默认图片工具初始化失败，改用获批只读加载本地图片。文档回写曾因审批传输中断未执行，核对后重试，不是代码或源故障。

## 下一步和限制

T-018a只完成旧资产池模拟配置页面边界，T-018/M4整体未完成。M1需确认PRD范围后冻结名单/指数/逐资产覆盖契约；300只、独立指数、概览、真实日更、数据恢复及PagesHTTPS仍未验收。原截图云日志未取得。云端不会自动获得本地修改，后续发布需当次授权。
