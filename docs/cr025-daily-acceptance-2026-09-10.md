# CR-025 连续实际交易日自动更新验收

截至2026-09-10：程序、离线门禁与自动调度已就绪；实际自动成功交易日为0，连续两日验收待真实运行，不能据此勾选MVP完成。

本任务自动化ID `300-mvp`，已通过自动化工具创建并核对配置为ACTIVE，每日上海时间07:30。首个计划09-11处理09-10行情，09-12处理09-11行情；机器开机且桌面app运行才可执行。来源：[官方自动化说明](https://learn.chatgpt.com/docs/automations?surface=app)。不另建任务或云端定时器。

执行命令（从仓库WSL目录）：

```sh
PYTHONPATH=services/algorithms/src:services/backend/src .venv/bin/python -m tools.daily_publication --trigger scheduled
```

零网络预检命令将末尾替换为 `--dry-run`，09-10实际结果为 `waiting_new_day`、目标09-09，没有创建日更目录或发起请求。真实候选发布前门禁已用CR-024包离线执行成功，证据 `artifacts/cr024-browser-20260910/daily-gate.json`，两策略指数回测均成功。

程序每交易日只尝试一次、最多4日；股票1500请求/3600秒，ETF135/1200秒，指数2/45秒，总上限1637请求/5400秒，最多6548请求。先写尝试账本再外部采集；互斥锁阻止并发。失败或中断日不自动重试，未完成running记录保守占预算。股票、ETF、指数全部完整，目标日期和一年区间匹配，五模块门禁通过，才原子切换 `artifacts/cr025-daily-20260910/publication/current.json`；初始回退为CR-024包，旧包与原服务不修改。新启动服务才读取新指针，不宣称热更新现有服务。

每次运行留存state.json、股票请求账本、ETF清单、指数响应与trace、五模块结果及发布内容哈希。相邻成功交易日（允许周末跨越）且trigger为scheduled才生成待审核标记；标记不能替代实际调度证明。最终须核对本任务两次调度实际触发时间与目标交易日，再回写两日通过。达到通过或四次耗尽后暂停自动化；异常通知用户，不追加预算。

离线测试覆盖日期相邻/周末/失败/手动不计数、dry-run不写入、失败保留旧指针、同日不重试、running保守计预算、错日期拒绝发布及指数越界在HTTP前拒绝。后续真实交易日事件如产生新未知缺口将阻断发布；只能按SDD核实证据，不删除检查或补造行情。

云端部署、HTTPS与许可验收保持独立未完成；本地定时器配置不代表生产运行。

实现边界补充：五模块门禁使用TESTING模式生成隔离临时任务库（系统临时目录），不碰已有任务库；持久采集、发布和验收证据仅写上述artifacts。最终离线375通过/8网络排除，其中日更门禁5测试通过。
