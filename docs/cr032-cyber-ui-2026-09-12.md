# CR-032 赛博朋克视觉与点选交互（2026-09-12）

本批用户授权UI与交互优化，先登记四份SDD；仅修改前端和验收工具，不改研究日期/算法/API契约或行情。新版尚未发布GitHub Pages，云端仍原UI对应服务；本地预览http://127.0.0.1:8767。

## 修改

- 深色网格底、青色主行动/数据高亮、紫色策略选中状态，面板细边框与光晕；图表使用深色主题，明确焦点与减少动态效果偏好，不闪烁。
- AssetPicker.vue统一相关性/回测/配置三个表单：名称或代码搜索、全部/股票/ETF筛选、原生复选框、已选名称/代码标签、逐项移除及清空、10项上限提示、空结果与未加载提示；筛选不清除选择，忙碌时禁用编辑，无需Ctrl多选。
- StrategyPicker.vue将两个策略的下拉框替换为原生单选说明卡，禁用未开放策略，参数仍跟随原metadata和校验。
- 首屏提供资产研究/策略实验室锚点入口，替换面向用户无意义的实现技术标签；手机端堆叠布局和可横向滚动导航。
- 文件：apps/frontend/src/App.vue、style.css、components/AssetPicker.vue、components/StrategyPicker.vue、styles/pickers.css、styles/dashboard.css。新增tools/verify_cr032_ui.cjs复验新的用户操作；旧浏览器脚本原select/input定位依赖旧UI，后续不可直接当新版验收脚本。

## 实际验证

`npm test --workspace @intelligent-quant/frontend`：57通过；`npm run build:frontend`成功，产物index-CvwNAXue.js/index-CkCfxOlY.css。

本地显式使用artifacts/cr024-publication-20260910完整327快照（截止09-09），独立artifacts/cr032-ui-20260912/jobs.db。未重新采集，也未把本地旧日期当云端回退；云端最近已验证批次仍为09-11。

真实Edge验证：327资产列表、代码搜索、无结果提示、筛选保留已选、27ETF筛选、已选移除、10项限制、键盘Space勾选、策略切换参数shortWindow联动通过；真实相关性POST200；820/390无横向溢出、pageErrors=[]。使用新UI提交本地回测202，任务a2a11a3e-0772-4c90-be0f-5d42b9701d6b succeeded。没有注入成功响应。

已查看首屏及390宽资产选择器截图，文字、对比度视觉和布局可读；未宣称正式WCAG认证或全场景人工审核。证据：artifacts/cr032-ui-20260912/browser/evidence.json、preview.png、desktop.png、viewport-820.png、viewport-390.png、backtest.png、picker-mobile.png。

命令：本地serve_publication_local.py --publication artifacts/cr024-publication-20260910 --jobs artifacts/cr032-ui-20260912/jobs.db --port 8767；Node tools/verify_cr032_ui.cjs <evidence-directory>，Playwright使用真实Edge。两次脚本执行环境问题分别为审批通信中断未执行、Python默认GBK读UTF-8失败；已改明确UTF-8，业务文件无部分损坏，补验通过。

## 边界与下一步

本地UI候选完成，可供用户预览，尚未推送/发布Pages。交互变更不影响两实际交易日自动更新验收的独立状态。没有新增采集或更改云端定时器，不承诺上游网络零失败。后续发布须走既有受保护main的PR流程，并在新页面重新验证浏览器数据接入。
