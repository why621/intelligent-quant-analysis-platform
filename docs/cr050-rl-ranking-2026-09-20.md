# CR050 四种RL实验排行

2026-09-20。用户要求排行榜纳入新算法；基线main c1d8d49。仅显式部署且依赖就绪的PPO/DQN/SAC/DDPG进入510300代表资产排行，仍为experimental；普通experimental/planned排除，不开放次日配置或实盘。

## 计算与契约

传统策略保留90自然日预热及默认本金10万元/费用。RL推理起点取原预热起点与样本外起点之较晚者，完整评价窗口不得与训练期重叠，首个评价净值点前须有20根预热。1d取最后两个实际行情点；其他周期按自然日截取，以窗口内首个净值点为收益基准。预热期收益/回撤/波动不纳入区间指标。四模型、名单、特征/执行版本及哈希校验不变。

RankingItem新增可选status/modelContext，RankingResponse新增unavailableStrategies说明训练重叠、预热不足或失败原因。完全无结果仍422，details附未参与列表，不补零，不截短1y冒充全年。缓存增加部署模型bundle标识，保留数据修订、单次并发保护及10秒预算。网页显示六策略、AI实验标识、样本外/费用说明与未参与原因。

## 本地证据

422算法/后端用例通过、2可选模块跳过、8network排除；78前端测试、Vite构建、修改核心Python和相关测试Ruff、diff检查通过。旧字段集合和缓存键断言同步新契约，未删除有效检查。

既有四真实模型与09-09不可变快照：1d/7d/30d各六条，逐项用独立回测净值重算区间收益、最大回撤、夏普均一致，排序及重复请求一致。30d首轮约2.285秒（本地观测）。1y因传统预热历史不够及四RL训练重叠整体422，六项原因明确，未放宽日期检查。Edge实际页面六行四AI标识，1440/390无页面横向溢出/pageerror；截图存档，不声称逐像素人工审阅。

实际命令：

```text
PYTHONPATH=services/algorithms/src:services/backend/src ../../.venv/bin/python -m pytest services/algorithms/tests services/backend/tests -q -o addopts= -m "not network"
npm run test --workspace @intelligent-quant/frontend
npm run build --workspace @intelligent-quant/frontend
# integration.py另启用既有rl-fix-deps/rl-fix-extra-deps及两模块src的PYTHONPATH，OMP/MKL=1
.venv/bin/python artifacts/cr050-rl-ranking/integration.py
node artifacts/cr050-rl-ranking/browser.cjs
```

证据在根工作区忽略目录artifacts/cr050-rl-ranking：backend-tests.log、frontend-tests.log、build.log、integration.log、ranking-local.json、browser-local/evidence.json。T033a/b完成；T033c云候选/部署/公网验证及T033d回写待执行。仅运行正确性与研究排行，不代表跨资产收益有效性；不重训、不上传权重/行情，Pages待分支合并。
