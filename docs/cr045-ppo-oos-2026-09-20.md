# 用户重训 PPO 的样本外验证（2026-09-20）

结论：模型加载与本地回测链路在本次检查范围内通过，绩效结论仅适用于下列短窗口；继续保留 experimental。

## 模型与数据

- 代码 608383bc0157b0a8d40668db134b194d6e26af86；模型 ppo-510300-v2-seed42，用户已完成实际训练，seed=42、20,000 timesteps；本轮只推理，不重新 fit。
- 模型格式 v2，executionVersion=account-feedback-v2；权重与元数据 bundleHash 已由 ModelStore 验证。模型 codeSha 与当前提交一致；publicationDate/dataVersion/universeVersion 与不可变快照逐项一致。
- 数据：本地已发布 Tencent 前复权快照 cr024-publication-20260910，publicationDate=2026-09-09；不是最新实时行情。数据版本 e5f794c7094c21c2ba08903c0c5697904f0d4b0f87a3312e6285f0f4c95634fe。
- 510300：训练 2025-09-09 至 2026-06-30，共192根；评估2026-07-01至2026-09-09，共51根。provider覆盖检查通过、日期有序唯一、价格有限。

## 比较口径

所有策略初始10万元、前20根预热不交易；最早决策2026-07-29，最早成交2026-07-30。全部51根净值保留，实际可交易段30根。为使起点一致，研究包装器屏蔽传统策略前20根信号；没有修改生产策略。买入持有仅在统一起点发出一次买入信号，不是7月1日起持有的价格涨幅。

费用沿用项目默认配置：单边佣金0.03%、卖出印花税参数0.05%、单边滑点0.02%。这是统一仿真参数，不声称等同ETF实际税费规则。表中手续费含佣金和该税参数，滑点已体现在成交价格中；期末持仓按末日收盘估值，未强制平仓。

| 策略 | 区间收益 | 最大回撤 | 成交笔数 | 手续费（元） | 可交易段平均仓位 |
| --- | ---: | ---: | ---: | ---: | ---: |
| PPO（本次新模型） | -0.3553% | 0.4821% | 9 | 166.61 | 8.10% |
| 均线交叉 5/20 | -3.2821% | 4.2616% | 2 | 107.44 | 33.33% |
| 动量反转 10/±5% | +0.0000% | 0.0000% | 0 | 0.00 | 0.00% |
| 买入持有（统一起点） | +0.1011% | 3.8534% | 1 | 30.00 | 100.00% |

PPO期末权益99,644.69元，净亏355.31元，其中实际记账手续费166.61元（非含反馈变化的无成本反事实）。它在此窗口亏损少于均线策略，但落后于未交易的现金和统一起点的买入持有。平均仓位约8.10%，小回撤不能单独证明风险调整后的优势。

动量反转0%来自没有触发有效买入，并非缺数或程序失败；活动决策信号计数：{'-1.0': 0, '0.0': 30, '1.0': 0}.

## 实际检查与可复现证据

- 模型内容/元数据完整性、三项数据上下文、训练截止日与代码版本均通过。
- PPO完整引擎回测重复两次，净值、成交、指标和逐bar决策跟踪完全一致；样本内窗口被 RLInSampleRequest 拒绝。
- 按成交记录独立重建现金与份额，再按每日收盘估值；四个策略均与引擎净值逐日一致。另行循环重算总收益和峰值回撤，与引擎指标一致；现金和份额非负。
- 运行期间禁止 socket 连接、禁止 PPO.learn；网络调用为0。模型zip、manifest、发布指针与发布包 SHA256 前后不变。无模型写入、无真实数据采集、无云操作。
- 脚本：artifacts/ppo-oos-20260920/run.py；结果：同目录 results.json，包含完整51日净值、每筆成交、指标、PPO实际持仓/目标权重、参数及检查结果。该目录被Git忽略。
- 复现（从修复工作树）：

```bash
PYTHONPATH=../rl-fix-deps:../rl-fix-extra-deps:services/algorithms/src \
  ../../.venv/bin/python artifacts/ppo-oos-20260920/run.py
```

命令成功退出，4.71秒；原始指标已保存，未将短窗口年化或夏普作为主要结论。当前缺少原生内联可视化运行时，使用表格及本地证据链接交付。

## 限制与下一步

单标的、单种子、短样本外区间；仅这一结果不能推断长期稳定性或优于基线。当前固定快照与归一化从窗口起点重建的口径需要在后续评估中保持一致。不要根据这51根反复调参后仍将其称为未见样本；扩展真实发布快照的独立验证/测试区间、多资产、多种子及训练收敛证据后再讨论 available。

本轮完成用户新模型的离线验证。没有训练其他三种算法、改动模型、注册网页、推送或部署。产物与文档留本地，修复提交608383b保持。
