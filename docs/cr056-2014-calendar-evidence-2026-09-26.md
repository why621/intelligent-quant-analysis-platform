# CR-056 实录：2014 年休市公告、检索边界的更正与 2015 训练下限的归属（2026-09-26）

分支 `algorithm/rl-long-window`，范围仅 `services/algorithms/**` 加根 SDD 与本文件。
触发原因是一句用户指认：「`https://www.sse.com.cn/disclosure/dealinstruc/closed/list/`
的第六页难道不是有 13 到 14 年的吗？」——有。CR-054/CR-055 写在风险清单里的
"2015 年之前仍无证据"因此被就地更正。

## 1. 结论

| 年份 | 采用的公告 | 公告口径休市工作日 | 表内 | 差异 | basis |
| --- | --- | --- | --- | --- | --- |
| 2014 | 年度通知（2013-12-25）+ 专项 〔2014〕2/4/6/10号 | 16 | 16 | 双向 0 | `official-notice`（新增） |

2015—2024 十年的 `closures` 与元数据一条未动：复算前后每年的公告休市工作日数
（17/17/16/18/17/19/18/18/18/20）与 CR-055 记录逐一相同，2015-01-05→2024-12-31 的
期望交易日仍是 2431 根。所以本轮和 CR-054/055 一样**不触发重填、不触发重训**，
已产出的四份研究权重与 manifest 继续有效。

## 2. 检索边界：这一页早就在我自己的记录里

栏目列表实测 6 页 84 条，第六页 9 条即整个栏目的最旧部分：

```
c_20160104_3979109  关于上海证券交易所2014年全年休市安排的通知   （2013-12-25）
c_20160104_3979115  关于2014年春节休市安排的公告                 上证公告〔2014〕2号
c_20160104_3979129  关于2014年清明节休市安排的公告               上证公告〔2014〕4号
c_20160104_3979131  关于2014年劳动节休市安排的公告               上证公告〔2014〕6号
c_20160104_3979175  关于2014年中秋节、国庆节休市安排的公告        上证公告〔2014〕10号
c_20160104_3979217  关于上海证券交易所2015年全年休市安排的通知    上证公告〔2014〕15号（CR-054 已采用）
c_20160104_3979239/263/272/306/337/4028872  2015 年各节日公告 + 2016 年度通知
c_20160104_3979100  关于2013年中秋节、国庆节休市安排的通知        ← 栏目到此为止（2013-09-11）
```

CR-054 实录第 1 节当时就写着"84 条（2013-09-11 → 2026-09-17）"——**这条事实一直在
仓库里，我没有从它推出 2014 可取证**。这是流程缺陷不是信息缺陷：范围结论应当由
已记录的事实推出，而不是顺手写进风险清单。D-12 就是把这条写成约束。

2013 年及更早确实取不到：栏目最旧一条只覆盖 2013 年中秋、国庆；它正文引用的
《关于上海证券交易所2013年全年休市安排的通知》（上证交字〔2012〕205号）不在 84 条之内；
「本所公告·一般公告」栏目实测是约 750 条的滚动窗口（第 50 页已落在 2026-07），
用它翻不到 2012 年；我不猜 URL。所以 2013—2012 的缺口这次是**交易所在线归档不可得**，
要补只能靠纸面公告或其他官方渠道，闸门行为不变（继续 `CalendarUnavailableError`）。

## 3. 年度通知不带文号时怎么写引用

2014 年的年度通知页面上**没有文号**，正文只有落款"二○一三年十二月二十五日"；它的
四份专项公告都把它引作《关于上海证券交易所2014年全年休市安排的通知》（上证函〔2013〕269号）。

CR-054/055 的引用格式是"标题+文号+发布日+URL"，复算工具和 network 用例都按这个形状
回抓并断言文号出现在页面上。为凑格式给这份通知写一个页面里不存在的文号，等于给
复核工具喂伪证——它会把"我编的文号"当成"页面确实如此"来通过。所以：

- 记录里写"（2013-12-25 发布）URL"，并另起一条说明"页面本身未标注文号"，
  同时给出四份专项公告对它的上证函引用作为身份佐证；
- 复算工具的 `CITATION` 从"必须带上证公告文号"改为通用三段式（标题 + 括注 + URL），
  锚定年份**优先取标题年份**、无标题年份（如「…70周年纪念日休市安排的公告」）再取
  文号年份；
- network 用例同样只在记录的括注里出现文号时才核对文号。

改完先复算再改数据：通用化之后、写入 2014 之前，十年输出与 CR-055 完全一致
（13 份公告、逐年数字不变），这才允许新增年份入库。

## 4. HISTORY_FLOOR 不动：需求边界不是日历的副产品

`splits.py` 里那句注释写的是"更早的K线需要一份我们未逐年核对的休市日历"，把
`HISTORY_FLOOR = 2015-01-01` 讲成了日历缺口的替代品。日历现在核到 2014，这条注释就
成了错误的因果——照它推理，今天该把下限挪到 2014，而 REQ-11 与助教口径都是
"**训练起点不早于 2015**"。所以本轮：

- `HISTORY_FLOOR` 保持 2015-01-01，注释改为真实理由（下限来自 REQ-11 本身；
  日历自 CR-056 起已核到 2014）；
- 想让训练真的跨过 2014，需要一次单独授权同时做两件事：放宽 REQ-11 的窗口规则、
  回填 2014 年K线（研究目录当前只到 2015-01-05→2024-12-31）。本轮两件都不做。

这是"需求边界"和"完整性红线"不得互相顶替的一个具体例子（D-12）。

## 5. 验证命令与结果

```bash
# 复算：11 个年份、18 份公告原文，全部双向零差异，exit 0
PYTHONUTF8=1 python services/algorithms/tests/golden/recheck_official_closures.py

# 回填前置检查（不落任何数据）
PYTHONUTF8=1 python -m quant_platform.data.deep_history --root <研究目录> --symbol 510300 \
  --start 2014-01-01 --end 2024-12-31 --preflight-only
# -> verifiedYears=[2014..2024] unverifiedYears=[] crossValidatedYears=[] ready=true

cd services/algorithms
PYTHONUTF8=1 python -m ruff check src tests          # All checks passed!
PYTHONUTF8=1 python -m pytest -o addopts="-q -m \"not network\""   # 337 passed, 14 deselected
PYTHONUTF8=1 python -m pytest tests/test_deep_history_probe.py -o addopts="-q"  # 6 passed（含 21 条引用逐份回抓）
cd ../backend && PYTHONUTF8=1 python -m pytest -q   # 146 passed + 3 errors（既有日期缺陷，未改后端）
```

2014 年的三方核对（写库前跑的只读探测）：公告 16 个休市工作日 = sina 候选 16 个，
双向 0 差异；腾讯 510300 2014-01-02→2014-12-31 共 245 根，落在休市日 0 根，
16 个休市工作日全部无K线。年度通知一份就给出全部 16 天，四份专项公告的区间无一超出它。

## 6. 测试改动清单

- `test_deep_history.py`
  `test_shipped_evidence_table_records_a_per_year_basis`：年份集合与 basis 映射扩到
  `range(2014, 2025)`，并断言 2014 记录含"未标注文号"与上证函〔2013〕269号；
  `test_preflight_reports_no_research_only_year_left_in_the_table`：preflight 起点改
  2014-01-01，新增 `closure_basis(2013) is None` 与 `unverified_years(2013-01-01,
  2014-12-31) == [2013]`；accept 用例参数化增加 2014-01-02/03 一窗（3 例）；
- `test_history_probe.py::test_probe_stays_in_officially_verified_calendar_years`：
  跨年拒绝窗从 `2014-12→2015-01` 改为 `2013-12→2014-01`，并把"2014 之前"改成
  "栏目归档到此为止"的表述；
- `test_coverage.py::test_calendar_and_interval_fail_closed`：越界窗从 2014 改 2013；
- `test_deep_history_probe.py`：观测 2 的取数窗改为 2014-01-01→2024-12-31，
  引用复核改为通用三段式并按需核对文号，条数下限从 13 提到 21。

拒绝路径的覆盖方式没变：`cross-validated` 靠临时副本降级钉住，未记录年份靠
2011/2013 这类真实落在归档之外的窗口钉住。

## 7. 风险与未完成

1. **2013 及更早**：交易所在线归档不可得（见 §2）。510300 2012-05-28 上市，理论上
   有 2012-06 起的K线，但没有可引用的官方休市原文就不入表。
2. **上证公告〔2020〕3号仍无 URL**（CR-055 遗留，不影响 2020 口径）。
3. **2014 未被任何数据使用**：日历放行 ≠ 数据可用，见 §4。
4. **多资产深历史 / T-035d 完整版**：未动，需要单独授权取数。
5. **后端接缝**（移交，不改代码）：`WebRL.model_context()` 的 `outOfSampleStartDate`
   与 `validate_web_request()` 仍按 `trainEndDate` 判样本内，应改用
   `quant_platform.rl.splits.in_sample_end(manifest)`；
   `services/backend/tests/test_live_data_acceptance.py` 的 fixture 仍只跳周末，应改
   `calendar.latest_session(date.today())`——本轮 3 个 errors 即此缺陷，与 CR-055 一致。
6. 仓库侧：`package-lock.json`（npm 版本差异造成的 `libc` 字段抖动）、`.zcode/` 与
   `services/backend/data/`（此前本机起服务联调留下的后端运行时缓存）有意未提交——
   真实行情缓存不入库。本轮未推送、未开 PR、未部署。RL 四策略继续 `experimental`。
