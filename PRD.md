# 产品需求文档

更新日期：2026-09-08；阶段：沪深300日频研究型MVP。变更入口：[spec.md](spec.md)。

## 产品定位与依据

面向项目团队、指导教师及教学研究演示用户，以可视化方式进行相关性分析、策略回测、策略比较与次日模拟配置。目标是可复现的研究流程，不承诺收益，不连接券商或自动下单。

依据为用户提供的《大创-量化 项目申请书》《需求文档》和最新讨论。原文指定Python+AkShare和收盘后日频采集，未指定必须使用东方财富；初始池为30—50只常见资产。原件不复制入仓库，避免带入个人信息。

用户确认前期沪深300、海外股票后置。默认运行仍为50只股票/ETF混合池；CR-012已验证官方300只名单及显式300股票+27ETF目录，尚未切生产池。完整行情覆盖、独立指数与概览仍待建设。需求编号表示目标，不表示已完成。

## 范围与阶段

- 目标方向：沪深300成分股日频数据与指数基准，最多10只资产的分析组合。
- 验证阶段：从已核实成分中选30—50只验证，不能将子集宣称为完整沪深300。
- 首发目标为完整300只当前成分，保留旧ETF入口；子集仅作为中间验证，完整覆盖须通过T-005。
- 收盘后刷新；展示实际交易日期、采集时间、来源和状态，不承诺盘中实时。
- 保留五类能力：概况、相关性、回测、排行、模拟配置。
- 后置海外股票、高频行情及AI训练；真实交易不在当前范围。

## 功能与验收标准

| 编号 | 需求 | 验收条件 |
| --- | --- | --- |
| REQ-01 | 可追溯资产池 | 名单有来源和生效/快照日期，代码唯一并含市场身份；完整模式核对300只，子集明确成员；所有资产可检索选择 |
| REQ-02 | 日线与质量 | 支持区间可获取OHLCV；日期有序无重复，复权及单位明确；区分停牌、新上市、漏数和上游失败，不伪造补价或静默返回缺失区间 |
| REQ-03 | 指数基准 | 沪深300指数与股票、ETF区分；策略/基准日期及收益口径可解释；不得无说明用510300ETF代替指数 |
| REQ-04 | 概况 | 已确认首发沪深300日频概况：成分涨跌/成交活跃度与沪深300指数，展示范围、日期、有效样本数；部分金额不冒充全量，成交活跃度不称净流入；原全市场/北向资金不作为本期必需指标 |
| REQ-05 | 相关性 | 2—10只不同资产；10只对应45对关系；日期、收益定义一致，数据不足明确报错 |
| REQ-06 | 回测 | 双均线、动量反转及区间选择；任务状态可查；收益曲线、回撤、夏普、Alpha/Beta或缺失原因明确；输入/成本/策略版本可追溯，不使用未来数据 |
| REQ-07 | 排行 | 同一资产池、区间、数据版本和成本比较；不足不发布误导排名，结果可与回测核对 |
| REQ-08 | 模拟配置 | 用明确的已完成交易日数据生成1—10只资产及现金权重，合计符合契约；标注截止日和模拟性质，过期/不足不冒充最新结果 |
| REQ-09 | 失败恢复 | 超时有界，保留有效旧数据；无快照明确不可用；历史与概览状态独立，缺失指标不填0 |
| REQ-10 | 公网与运维 | Pages经有效HTTPS访问云API，CORS指定来源；浏览器五模块验收；持久化、调度及备份恢复验证，不只检查health |

REQ-05至REQ-09已有部分或主要实现；新的范围与真实数据验收仍需验证，任务状态见 [task.md](task.md)。

## 质量底线

CR-013验收补充：逐资产覆盖按完整请求区间交易日集合核验，首尾完整不能掩盖中间缺口。未采集、源失败、无数据、质量无效、覆盖缺口和完整价格覆盖分别记录；金额缺失单独计数，complete仅指所请求前复权OHLCV日期/基本数值检查通过，不证明复权经济正确、全量概览或停牌原因。固定名单最近一年不允许用缺数据资产静默缩小最终300只分母。

CR-013实测：六只返回，五只完整；中芯国际2025-09-08缺口有官方停牌解释，自动停牌分类和研究执行规则尚未实现。完整300目标不变；不能将停牌改成补价、直接删除或更换资产。结果与下一步见 [覆盖报告](docs/cr013-history-validation-2026-09-08.md)。

- HTTP200、容器healthy和基础烟测不代替业务验收。
- 页面读已校验缓存，不因每次刷新启动全市场采集；回测保持异步。
- 超时、并发和缓存失效要有测试；记录实际负载，不凭机器规格承诺并发人数。
- 不直接拼接不同源、单位或复权基准的序列。
- 区分当前固定池与历史时点成分池，披露幸存者偏差限制。
- 授权按个人、团队、公开展示/再分发分别确认；开源许可不等于行情授权。
- 密钥、原始个人材料、数据库及真实行情缓存不提交仓库。

## 已确认决策与待确认事项

| 编号 | 决策 | 未确认前边界 |
| --- | --- | --- |
| Q-01 | 已确认：完整300只当前成分，保留ETF | 采样不是最终验收；ETF不计入300只，不删除旧数据 |
| Q-02 | 已确认：固定当前名单、最近一年回测 | 披露幸存者偏差，不称历史指数成分回测；当前日历仅2025—2026 |
| Q-03 | 已确认：首页沪深300日频概览 | 明示范围、日期与覆盖；成交活跃度不称净流入，不冒充全市场 |
| Q-04 | 已确认：团队内部教学演示；源许可审查未完成 | 不代表数据许可已取得；不购买账号、不公开发布 |
| Q-05 | 域名、HTTPS接入与发布时间 | 保持临时回归入口，不将HTTP API接入Pages |

## 2026-09-08实现进展（不变更范围）

REQ-02/09的腾讯000个股单位与旧缓存保护、REQ-08的保守交易日截止/缺失拒绝/现金守恒已在本地实现并验证。两只股票加ETF的真实缓存与研究API链路通过，证据见 [验证报告](docs/research-chain-validation-2026-09-08.md)。REQ-01完整范围、REQ-03指数身份、REQ-04概览和REQ-10公网验收仍未完成；不能由子集测试认定MVP完成。

### 云端进展

CR-015用户授权先查看现有可运行功能的云端预览。部署范围仍为旧50只混合池的研究功能，不代表完整300只、首页概览、HTTPS或最终公开发布已验收；不改变完整MVP的REQ-01至10目标。

2026-09-08 19:18 CR-014：香港云服务器实际腾讯日线复测六只成功，18次HTTP全部200、与本地1451条一致；单只停牌缺口仍保留。仅更新REQ-02源可达证据，不代表全300、首页或REQ-10完整MVP验收；没有部署/日更生产数据。见 [只读复测报告](docs/cr014-cloud-history-2026-09-08.md)。

2026-09-08：用户授权后已将现有修复部署云端，完成五只旧缓存迁移和HTTP异步研究链路验证，见 [部署记录](docs/cloud-deployment-2026-09-08.md)。当前仍50只旧混合池，REQ-01/03新范围、REQ-04概览、REQ-10完整公网与运维验收未完成；不改变上述待确认范围。

## CR-007补充的用户操作验收

- REQ-05/06：首次打开页面、使用默认日期和默认策略参数，即可在已发布数据范围内完成一次分析及回测；不得要求用户修改请求JSON或手填隐藏参数。数据尚未就绪时禁用提交并说明原因。
- REQ-02/05/06：显示实际可用日期，截止不得默认取尚未发布的当天；切换资产后按共同可用范围校验。用户主动选择越界区间必须给出明确错误，不静默改变已提交区间。
- REQ-03/06：页面基准名称、类型、请求身份及回测结果一致；指数适配完成前不发送未经支持的000300。过渡方案仅允许明确标注的ETF或明确无基准，不冒充指数。
- REQ-04：正常数据条件下必需概览卡片必须有真实结果、范围、日期及覆盖率。可选指标缺失可解释，不能把所有卡片永久占位称为完成。
- REQ-10：真实浏览器从页面按钮跑完五模块，包括输入参数、等待任务、图表渲染、错误反馈及刷新恢复；curl/手写脚本成功不能替代。

实施顺序及待确认边界见 [MVP计划](docs/mvp-implementation-plan.md)。推荐完整300只作为目标验收、30—50只作为中间验证批次；首发范围、固定名单回测、首页统计和数据使用授权仍以Q-01至04确认结果为准，不能在文档中自动批准。

## MVP完成定义

CR-012进展：REQ-01名单可追溯及目录API验证已完成，源文件日期2026-09-07，300唯一股票；不等于REQ-02一年行情或REQ-03指数验收。真实源文件/哈希/质量检查按流程保存，完整剩余项见 [本轮报告](docs/cr012-universe-validation-2026-09-08.md)。

批准并明确标注的范围满足REQ-01至REQ-10；选定源云端验证、业务及故障场景通过；未完成能力明确标记；T-009记录版本、日期、环境和限制。范围收缩先更新需求，不能删验收项掩盖缺失能力。

### CR-009 M0本批验收边界

两策略参数来自服务端Schema，必填/数值/边界/关系不合法时禁用；无metadata或发布截止时禁用。首次日期按已发布截止初始化，异步刷新不覆盖用户编辑。M0展示全局截止并明确逐资产完整性仍须服务端校验，M1发布每资产覆盖目录。越界请求返回可理解的请求/可用日期；不将范围内源失败都改成日期错误。默认无基准（Alpha/Beta为空），用户可显式选目录中的ETF并显示类型。任务编号可恢复查询，刷新后结果关联原始请求，忙碌和卸载不会导致重复提交或旧响应覆盖。
### CR-009验收进展

2026-09-08：REQ-05/06页面默认相关性、两策略参数、日期越界提示、任务恢复在三只真实ETF样本上通过；REQ-03过渡ETF/无基准明确，比较曲线统一首日=1。相关性null留空，未伪造数据。T-014完成仅指M0本地边界，REQ-01/03独立指数/04概览/10公网与日更仍未完成；Q-01至05保留待确认。详见 [M0报告](docs/m0-validation-2026-09-08.md)。

### CR-010既有模拟配置验收补充

REQ-08输入必须为1—10只不同六位资产和0—100的有限现金百分比，清空现金不能当作0提交。重复点击不能清掉运行状态；组件退出后忽略晚响应。成功结果明确显示实际现金权重，合法全现金结果不得显示“等待生成”；不改变策略信号、日期或后端权重算法。

CR-010本地边界已实现并通过前端离线与浏览器验收；仍使用已有真实ETF隔离缓存，非全量数据/云发布验收，见 [续作报告](docs/cr010-followup-2026-09-08.md)。
- CR-011最新状态（2026-09-08）：Q-01至03及内部演示用途已确认，覆盖下文历史待确认说明。REQ-01目录基础已通过合成300只分页测试，但真实完整成员和行情仍未接入；REQ-03独立指数、REQ-04新日频概览、许可与公开发布未完成。验收见 [CR-011报告](docs/cr011-validation-2026-09-08.md)。
