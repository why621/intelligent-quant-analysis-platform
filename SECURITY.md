# 安全策略

请不要在公开 Issue 中提交密钥、账户信息、真实交易记录或可利用的漏洞细节。

发现密钥泄露时，立即在对应平台吊销并轮换密钥，再清理 Git 历史和部署环境。仅删除当前分支中的文件不能消除泄露。

合并代码前应确认：

- 环境变量文件未被提交。
- 日志和错误响应不包含 Token、个人信息或交易凭据。
- 新依赖来源可信且版本已锁定。
- 对外接口包含鉴权、输入校验、限流和审计设计。
- 实盘动作具备风控限制与人工确认。
